<%= form_for(@role_in_an_episode) do |f| %>
  <% if @role_in_an_episode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_in_an_episode.errors.count, "error") %> prohibited this role_in_an_episode from being saved:</h2>

      <ul>
      <% @role_in_an_episode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :actor_id %><br />
    <%= f.number_field :actor_id %>
  </div>
  <div class="field">
    <%= f.label :episode_id %><br />
    <%= f.number_field :episode_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
