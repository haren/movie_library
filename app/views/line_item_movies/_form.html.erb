<%= form_for(@line_item_movie) do |f| %>
  <% if @line_item_movie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@line_item_movie.errors.count, "error") %> prohibited this line_item_movie from being saved:</h2>

      <ul>
      <% @line_item_movie.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movie_id %><br />
    <%= f.number_field :movie_id %>
  </div>
  <div class="field">
    <%= f.label :cart_id %><br />
    <%= f.number_field :cart_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
